import { css } from 'styled-components'

import { SPACING } from '@opentrons/components'

/**
 * Styles
 */

export const LIST_CONTAINER_STYLE = css`
  padding: ${SPACING.spacing32} ${SPACING.spacing60};
  grid: ${SPACING.spacing24};
`
